<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | Sweet Home Real Estate Investments</title>
  <link rel="stylesheet" href="/css/variables.css" />
  <link rel="icon" href="/images/favicon.png" type="image/png">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    /* Align with site auth look & CSS variables */
    body {
      font-family: var(--font-sans);
      background:
        radial-gradient(1200px 800px at 10% -10%, rgba(0,0,0,0.05), transparent),
        radial-gradient(1200px 800px at 110% 110%, rgba(0,0,0,0.04), transparent),
        var(--white);
      min-height: 100vh;
      display: grid;
      place-items: center;
      padding: var(--spacing-lg);
    }

    .role-container { width: 100%; max-width: 960px; animation: fadeIn 0.4s ease; }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .header-section { text-align: center; margin-bottom: var(--spacing-16); }
    .logo { max-width: 160px; height: auto; margin-bottom: var(--spacing-6); }
    .header-section h1 { color: var(--color-primary); font-size: var(--font-size-4xl); font-weight: 700; margin-bottom: var(--spacing-2); }
    .header-section .subtitle { color: var(--color-secondary); font-size: var(--font-size-lg); }

    .role-selection {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      margin-bottom: 2rem;
    }

    .role-card {
      background: var(--white);
      border: 1px solid rgba(0,0,0,0.08);
      border-radius: var(--card-radius);
      padding: var(--spacing-12) var(--spacing-10);
      cursor: pointer;
      transition: transform var(--transition-fast), box-shadow var(--transition-fast), border-color var(--transition-fast);
      text-align: center;
      width: 100%;
      position: relative;
      overflow: hidden;
      box-shadow: var(--card-shadow);
    }

    .role-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 4px;
      background: linear-gradient(90deg, var(--primary-color), var(--primary-dark));
      transform: scaleX(0);
      transform-origin: left;
      transition: transform var(--transition-normal);
    }

    .role-card:hover::before {
      transform: scaleX(1);
    }

    .role-card:hover { transform: translateY(-6px); box-shadow: var(--shadow-xl); border-color: rgba(0,0,0,0.12); }

    .role-card:active {
      transform: translateY(-4px) scale(1.01);
    }

    .role-icon { font-size: 3rem; margin-bottom: var(--spacing-4); display: inline-block; animation: float 3s ease-in-out infinite; }

    @keyframes float {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }

    .role-card:nth-child(1) .role-icon {
      animation-delay: 0s;
    }

    .role-card:nth-child(2) .role-icon {
      animation-delay: 0.3s;
    }

    .role-card h3 { color: var(--color-primary); margin-bottom: var(--spacing-2); font-size: var(--font-size-2xl); font-weight: 700; }
    .role-card p { color: var(--color-secondary); font-size: var(--font-size-base); line-height: 1.6; margin: 0; }

    .role-badge { display: inline-block; margin-top: var(--spacing-3); padding: .35rem .9rem; background: var(--primary-color); color: #fff; border-radius: var(--radius-full); font-size: var(--font-size-sm); font-weight: 600; letter-spacing: .4px; }

    @media (max-width: 768px) {
      body { padding: var(--spacing); }
      .header-section h1 { font-size: var(--font-size-3xl); }
      .header-section .subtitle { font-size: var(--font-size-base); }
      .role-selection { grid-template-columns: 1fr; gap: var(--spacing-6); }
      .role-card { padding: var(--spacing-10) var(--spacing-8); }
      .role-icon { font-size: 2.75rem; }
      .logo { max-width: 140px; }
    }

    @media (max-width: 480px) {
      .header-section h1 { font-size: var(--font-size-2xl); }
      .role-card h3 { font-size: var(--font-size-xl); }
      .role-card p { font-size: var(--font-size-sm); }
    }
  </style>
</head>
<body>
  <div class="role-container">
    <div class="header-section">
      <img src="/images/Sweet Home Logo.png" alt="Sweet Home Logo" class="logo" />
      <h1>Select Your Role</h1>
      <p class="subtitle">Welcome back, <%= user.name %>! Choose how you want to access the dashboard.</p>
    </div>

    <form action="/auth/select-role" method="POST">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      <div class="role-selection">
        <button type="submit" name="role" value="SuperAdmin" class="role-card">
          <div class="role-icon">👑</div>
          <h3>SuperAdmin</h3>
          <p>Full access to all features, team management, and system settings</p>
          <span class="role-badge">Full Access</span>
        </button>

        <button type="submit" name="role" value="Admin" class="role-card">
          <div class="role-icon">👤</div>
          <h3>Admin</h3>
          <p>Manage your properties and projects with standard permissions</p>
          <span class="role-badge">Standard Access</span>
        </button>
      </div>
    </form>
  </div>
</body>
</html>

